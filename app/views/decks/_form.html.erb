<%= form_with(model: deck, local: true) do |form| %>
  <% if deck.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deck.errors.count, "error") %> prohibited this deck from being saved:</h2>

      <ul>
      <% deck.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :pilot %>
    <%= form.text_field :pilot %>
  </div>

  <div class="field">
    <%= form.label :solo_score %>
    <%= form.number_field :solo_score %>
  </div>

  <div class="field">
    <%= form.label :group_score %>
    <%= form.number_field :group_score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>